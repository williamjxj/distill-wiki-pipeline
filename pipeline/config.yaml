wiki_root: ../wiki
parent_root: ..
llm:
  default: ollama
  ollama_base_url: http://localhost:11434
  models:
    ollama: deepseek-v4-flash:cloud
  overrides:
    export_brief: ollama
server:
  host: 127.0.0.1
  port: 8787
